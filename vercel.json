{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --prefix frontend && npm install --prefix backend --omit=optional",
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/records", "destination": "/api" },
    { "source": "/records/:path*", "destination": "/api" },
    { "source": "/records-summary/:path*", "destination": "/api" },
    { "source": "/sizo-summary/:path*", "destination": "/api" },
    { "source": "/sizo-image", "destination": "/api" },
    { "source": "/sizo-image/:path*", "destination": "/api" },
    { "source": "/photos", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 10,
      "includeFiles": "backend/**"
    }
  }
}
